{"iter": 1, "hyp_id": "phase3_013_iter_001", "status": "reject", "capability_investigated": "WhisperProcessor feature extractor with a list input, and the batched\nprompt/result contract of frozen.asr_backend.generate (one prompt row,\none result row per input window).", "what_i_learned": "The processor accepts a list of raw-audio chunks and returns input_features\nshaped (N, mels, frames); generate accepts [prompt]*N and returns N results\n(results[i].sequences_ids[0]). The stub fed only the first 30 s, so all audio\npast 0:30 was silently dropped — a pure deletion sink on long-form calls.", "hypothesis": "Window the full audio into non-overlapping 30 s segments, decode them in one\nbatched generate call, and concatenate the per-window text. This recovers the\ntruncated tail that the single-window stub deleted, which should sharply cut\ncorpus_cer on the long-form 0715 batch.", "fingerprint": ["windowing", "batched", "chunks", "longform", "deletion"]}
{"iter": 2, "hyp_id": "phase3_013_iter_002", "status": "keep", "capability_investigated": "The VRAM cost model of frozen.asr_backend.generate under a batched prompt\nlist — what CT2 Whisper allocates when given (N, mels, frames) features vs\na single row.", "what_i_learned": "generate decodes the whole prompt batch concurrently, so passing all N 30 s\nwindows in one call allocates encoder+decoder state for every row at once and\nOOMs on long calls. Feeding one window per generate call caps peak VRAM at\nsingle-window cost while still recovering all audio past 0:30; the batched\nlist contract works but is not memory-safe for long-form here.", "hypothesis": "Loop the windows sequentially — one processor() + generate() per 30 s chunk —\nand join the decoded text. This fixes the iter-1 OOM crash while preserving\nits deletion-sink fix, so the candidate actually scores.", "fingerprint": ["windowing", "sequential", "oom", "longform", "perchunk"], "corpus_cer": 0.41135005140895387}
{"iter": 3, "hyp_id": "phase3_013_iter_003", "status": "reject", "capability_investigated": "The `prompts` argument of frozen.asr_backend.generate — specifically whether\nthe prompt token-id list can carry a `<|startofprev|>` continuity segment\nbefore `<|startoftranscript|>`, and what results[0].sequences_ids[0] returns\nthat can be fed back as that prefix.", "what_i_learned": "generate's prompt is a flat list of token ids, so it accepts more than the\nstatic SOT prefix: prepending the `<|startofprev|>` id plus the previous\nwindow's decoded sequences_ids gives the decoder cross-window context — the\nnative Whisper long-form conditioning path, which the pipeline had never\nused. Until now every 30 s window decoded context-blind from a cold start.", "hypothesis": "Carry each window's decoded token ids forward (capped to 200 tokens) as a\n`<|startofprev|>` prefix on the next window. Restoring continuity across the\nhard 30 s cuts should reduce the cold-start drift/repetition collapse that\ntruncates windows early, lifting length_ratio and cutting the 80% deletion\naxis — strongest on continuous-speech files like the 233 s-span worst case.", "fingerprint": ["prompt", "startofprev", "condition", "prev-context", "longform"], "corpus_cer": 0.4922015230991757}
