{"iter": 1, "hyp_id": "phase3_012_iter_001", "status": "reject", "capability_investigated": "generate(features, prompts, ...) signature and the WhisperFeatureExtractor\noutput shape — specifically whether the decode call accepts a batched\nStorageView (N x n_mels x 3000) with a parallel prompt list rather than a\nsingle 30s window.", "what_i_learned": "The processor pads/truncates every input to a fixed 30s mel window, so the\nstub silently dropped everything past the first 30 seconds of each call\n(pure deletion). generate takes a list of prompts, so a batched feature\nStorageView with one prompt per window decodes all segments in a single\ncall and returns one result per window.", "hypothesis": "Split audio into ceil(len/30s) non-overlapping 30s windows, extract all\nfeatures at once, run one batched generate over [prompt]*N, and join the\nper-window transcripts. This recovers the deleted tail and should sharply\nraise length_ratio.p05 / cut the deletion rate that dominates the stub.", "fingerprint": ["chunking", "batched-generate", "longform", "windowing"]}
{"iter": 2, "hyp_id": "phase3_012_iter_002", "status": "keep", "capability_investigated": "Re-examined generate()'s memory behavior on a batched StorageView: how peak\nGPU allocation scales with the number of 30s windows passed in one decode\ncall (N x n_mels x 3000 features + N parallel beams).", "what_i_learned": "generate() decodes all prompts in the list concurrently, so a single batched\ncall over every window of a long call allocates memory proportional to the\nwindow count — long 0715 calls have enough windows to exceed GPU memory\n(CUDA out of memory). Decoding one window per generate() call bounds peak\nmemory to a single 30s window and still recovers the full tail.", "hypothesis": "Keep the long-form windowing that fixed the tail-deletion, but iterate the\nwindows through generate() one at a time instead of one giant batched call;\nthis removes the OOM crash while preserving the deletion fix, so the\niteration actually scores.", "fingerprint": ["sequential-windows", "oom-fix", "per-window-decode"], "corpus_cer": 0.41135005140895387}
