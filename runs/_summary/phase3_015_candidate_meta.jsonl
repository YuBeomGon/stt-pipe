{"iter": 1, "hyp_id": "phase3_015_iter_001", "status": "reject", "capability_investigated": "WhisperProcessor feature extraction in the load()->processor path: it pads/\ntruncates every input to a fixed 3000-frame (30 s) mel window, and generate()\naccepts a batched feature StorageView with one prompt per batch row.", "what_i_learned": "The processor silently caps each call at one 30 s window, so the stub deleted\nthe entire tail of any recording longer than 30 s. The backend's generate()\ntakes a stacked (N, n_mels, 3000) feature batch plus an N-length prompt list\nand returns one result per window, letting a whole long-form clip be decoded\nin a single batched call rather than one truncated window.", "hypothesis": "Splitting the waveform into consecutive 30 s windows and concatenating the\nper-window transcripts recovers the deleted tail, which should collapse the\ndeletion-dominated CER far more than any decode-knob tweak on a single window.", "fingerprint": ["chunking", "longform", "windowing", "batched-generate"]}
