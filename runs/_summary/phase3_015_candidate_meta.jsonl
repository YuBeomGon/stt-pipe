{"iter": 1, "hyp_id": "phase3_015_iter_001", "status": "reject", "capability_investigated": "WhisperProcessor feature extraction in the load()->processor path: it pads/\ntruncates every input to a fixed 3000-frame (30 s) mel window, and generate()\naccepts a batched feature StorageView with one prompt per batch row.", "what_i_learned": "The processor silently caps each call at one 30 s window, so the stub deleted\nthe entire tail of any recording longer than 30 s. The backend's generate()\ntakes a stacked (N, n_mels, 3000) feature batch plus an N-length prompt list\nand returns one result per window, letting a whole long-form clip be decoded\nin a single batched call rather than one truncated window.", "hypothesis": "Splitting the waveform into consecutive 30 s windows and concatenating the\nper-window transcripts recovers the deleted tail, which should collapse the\ndeletion-dominated CER far more than any decode-knob tweak on a single window.", "fingerprint": ["chunking", "longform", "windowing", "batched-generate"]}
{"iter": 2, "hyp_id": "phase3_015_iter_002", "status": "keep", "capability_investigated": "generate()'s memory behaviour under a batched (N, n_mels, 3000) feature\nStorageView vs. one-window-at-a-time calls — i.e. how peak GPU memory scales\nwith the number of stacked prompts in a single generate() call.", "what_i_learned": "The backend's batched generate() allocates decode state for ALL N windows at\nonce, so a long clip (many 30 s windows stacked into one call) exhausts CUDA\nmemory — the failure was OOM, not a shape/contract bug. Decoding the same\nwindows sequentially (N separate single-row generate calls) is numerically\nequivalent for the transcript but caps peak memory at one 30 s window.", "hypothesis": "Keep iter_001's long-form windowing (split waveform into <=30 s chunks so the\ntail is no longer deleted) but decode each chunk in its own generate() call\nand join the pieces, fixing the CUDA-OOM crash while preserving the tail\ncoverage that motivated the change.", "fingerprint": ["longform", "windowing", "sequential", "oom-fix"], "corpus_cer": 0.41135005140895387}
{"iter": 3, "hyp_id": "phase3_015_iter_003", "status": "keep", "capability_investigated": "The decode return channel when <|notimestamps|> is omitted from the prompt:\ngenerate()'s sequences_ids then contain Whisper timestamp tokens (the high\nend of the vocab, >= the <|0.00|> id), each step = 0.02 s. The fixed-stride\npipeline suppressed and discarded these entirely.", "what_i_learned": "Timestamp tokens are recoverable from results[0].sequences_ids without any\nbackend flag — just drop <|notimestamps|> from the prompt and read token ids\n>= tokenizer.convert_tokens_to_ids(\"<|0.00|>\"); their value gives the time\nthe model attended to inside each 30 s window. Linguistic tokens are all\nbelow that id, so text and timestamps separate cleanly by a single id cutoff.", "hypothesis": "Deletion (80%, length_ratio 0.65) is driven by blind 30 s strides skipping\naudio when a window's content ends early (EOT/repetition collapse before\n30 s). Steering the next window's start to the model's own last timestamp\ninstead of seek+30 s stops skipping unread tails and should raise coverage.", "fingerprint": ["timestamps", "seek", "longform", "sliding-window"], "corpus_cer": 0.17573148841991532}
