{"iter": 1, "hyp_id": "phase3_007_iter_001", "status": "reject", "capability_investigated": "WhisperProcessor feature extractor framing (fixed 30s mel window) and the\nbatch interface of frozen.asr_backend.generate — it accepts an (N, n_mels,\nn_frames) StorageView with a list of N prompts and returns N results.", "what_i_learned": "The extractor pads/truncates any input to exactly one 30s window, so the\nstub silently dropped everything after 0:30 of each call (structural\ndeletion, length_ratio floored). generate() is batched over the leading\naxis, so multiple 30s windows can be decoded in one call and concatenated.", "hypothesis": "Split the waveform into consecutive non-overlapping 30s windows, extract mel\nfeatures per window, decode them all in one batched generate() call, and join\nthe per-window text in order. This recovers the deleted tail — the dominant\nerror axis for multi-minute 0715 calls — without touching decode params.", "fingerprint": ["longform", "windowing", "30s", "batched", "chunking"]}
{"iter": 2, "hyp_id": "phase3_007_iter_002", "status": "keep", "capability_investigated": "GPU memory behavior of frozen.asr_backend.generate when fed an\n(N, n_mels, n_frames) batch — the OOM crash from iter_001's all-windows-in-\none-call batch on multi-minute 0715 calls.", "what_i_learned": "The batched generate() over the leading axis allocates decode state for all\nN windows at once on a float16 CUDA model; long calls (many 30s windows)\nexceed GPU memory and raise \"CUDA failed with error out of memory\". Peak\nmemory scales with batch size, so decoding windows sequentially (batch=1)\nkeeps the structural-deletion fix while staying within VRAM.", "hypothesis": "Keep the 30s windowing that recovers the truncated tail, but iterate windows\none at a time through generate() and concatenate the per-window text. This\nremoves the OOM crash while preserving the length-ratio recovery.", "fingerprint": ["longform", "windowing", "sequential", "oom-fix", "per-window"], "corpus_cer": 0.408675043131241}
{"iter": 3, "hyp_id": "phase3_007_iter_003", "status": "reject", "capability_investigated": "generate()'s **decoding_kwargs surface — specifically the beam-search and\nanti-repetition knobs the frozen docstring advertises (beam_size, patience,\nrepetition_penalty) plus no_repeat_ngram_size, none of which the parent\npipeline uses (it decodes pure-greedy beam_size=1, temp=0.0).", "what_i_learned": "The parent's per-window deletion is not purely structural windowing loss:\n4/12 files carry the repeated_text flag (repeated 0.27) while length_ratio\nsits at 0.65. That is the classic greedy long-form Whisper failure — the\ndecoder enters a repetition loop, spends the per-window token budget on a\nrepeated run, and the collapsed run reads as deletion. So the repetition\naxis and the coverage axis share one cause, addressable purely through\ndecode params with no new mechanism.", "hypothesis": "Replace greedy decode with beam_size=5 + patience=1.0 to stop premature\ncollapse, and add repetition_penalty=1.15 + no_repeat_ngram_size=3 to break\nloops before they consume the budget. This should raise length_ratio and\ncut the repeated_text rate, lowering corpus_cer. Cost ~5x inference\n(105s parent → ~525s) stays under the 719.9s budget.", "fingerprint": ["beam", "repetition-penalty", "no-repeat-ngram", "patience", "decode-tune"], "corpus_cer": 0.4656431347263127}
{"iter": 4, "hyp_id": "phase3_007_iter_004", "status": "keep", "capability_investigated": "The prompt-token side of generate()'s decode path — specifically the\n<|notimestamps|> special token that every prior iteration pinned in the\nforced prompt, and the alternative timestamp-decoding regime that omitting it\nunlocks (the decoder then emits <|t|> segment-boundary tokens in the returned\nsequences_ids). Never used in the ledger, which only covered mel framing,\nbatched-OOM, and beam/repetition decode kwargs.", "what_i_learned": "Coverage loss is not just blind 30s cutting — in <|notimestamps|> greedy mode\nlarge-v3 emits EOT early within a window on dense multi-utterance audio,\ntranscribing only the front and stopping, which is exactly the length_ratio\n0.65 / deletion-80% signature. The frozen surface lets me switch to the\ntrained timestamp regime purely by dropping <|notimestamps|> from the forced\nprompt; the returned sequences_ids then carry timestamp tokens (every id >=\n<|0.00|>) which I filter out before tokenizer.decode so they don't pollute\ntext. Stride stays fixed 30s, so decode-call count and runtime are unchanged.", "hypothesis": "Decoding each 30s window in timestamp mode (prompt without <|notimestamps|>)\nkeeps the model transcribing to the window end instead of terminating early,\nraising length_ratio and cutting the dominant deletion axis without adding\nwindows or risking the runtime budget. Timestamp tokens are stripped from\nsequences_ids before text decode.", "fingerprint": ["timestamps", "notimestamps-drop", "coverage", "early-eot", "prompt-tokens"], "corpus_cer": 0.34762037537249707}
{"iter": 5, "hyp_id": "phase3_007_iter_005", "status": "reject", "capability_investigated": "The numeric value of the timestamp tokens in generate()'s returned\nsequences_ids — not their presence (family_003 already established that\ndropping <|notimestamps|> makes them appear), but that each id decodes to a\nwall-clock offset via (id - timestamp_begin) * 0.02, letting me locate where\neach emitted segment sits inside the 30s window.", "what_i_learned": "family_003's hallucination regression (0.091 -> 0.182) rides on the feature\nextractor's silent padding of the final short window: in timestamp mode\nlarge-v3 keeps emitting <|t|> segments past the real audio and invents text\nfor the padding. The timestamp values family_003 discarded are exactly the\nsignal to gate that — a segment whose start offset >= len(chunk)/sr is\npadding-region and can be dropped, with full 30s windows unaffected.", "hypothesis": "Keep timestamp decoding (family_003 coverage) but parse segment boundaries\nand keep only text whose segment starts before real_seconds = len(chunk)/sr,\ncomposing family_003's length_ratio win with family_001's lower hallucination\ninstead of trading one for the other.", "fingerprint": ["timestamp-segments", "padding-guard", "hallucination", "combine"], "corpus_cer": 0.34762037537249707}
{"iter": 6, "hyp_id": "phase3_007_iter_006", "status": "keep", "capability_investigated": "generate()'s returned timestamp tokens used as a SEEK-FEEDBACK signal, not\njust text/padding filtering. Prior iters (004/005) established that dropping\n<|notimestamps|> makes <|t|> tokens appear and that their offset is\n(id - timestamp_begin) * 0.02. This iter investigates using the LAST emitted\nsegment's end offset to set the next window's start, replacing the fixed 30s\nstride — the standard long-form Whisper seek loop, never driven before.", "what_i_learned": "The best's length_ratio 0.74 is structurally explained by fixed striding:\nin timestamp mode large-v3 still EOTs partway through a 30s window (~22s on\ndense audio), and a fixed 30s stride permanently skips the un-transcribed\n~8s tail of every window (22/30 ≈ 0.73 ≈ observed 0.74). The decoder stamps\nwhere it actually stopped via the last timestamp token, so that stop point\nis recoverable as a seek target rather than a silent loss.", "hypothesis": "Advancing seek to the last segment's end offset (floored at 20s to bound\nwindow-count/runtime) re-reads the skipped tail in the next window, lifting\nlength_ratio toward 1.0 and cutting the dominant deletion (76%) without a\nnew decode regime — purely by closing the gap fixed striding bakes in.", "fingerprint": ["adaptive-seek", "timestamp-stride", "last-segment", "coverage", "long-form"], "corpus_cer": 0.20948713033476812}
{"iter": 7, "hyp_id": "phase3_007_iter_007", "status": "reject", "capability_investigated": "The advance-clamp expression in the adaptive seek loop and the numeric range\nof generate()'s returned timestamp tokens — specifically the upper bound of\n(ts_tokens[-1] - timestamp_begin)*0.02*sr that the parent wraps in\nmin(max(advance, min_advance), window).", "what_i_learned": "The Whisper timestamp vocabulary spans only <|0.00|> through <|30.00|> within\na single 30s window, so the largest possible last-timestamp offset is exactly\n30.0s = _WINDOW_SECONDS. Therefore advance = (id - begin)*0.02*sr is bounded\nabove by `window` by construction, and the outer min(..., window) clamp is\ndead code that can never bind. The min_advance floor is the only load-bearing\nguard (it prevents a near-zero advance from blowing up the decode-call count).", "hypothesis": "Ablate the redundant upper clamp: replace min(max(advance, min_advance),\nwindow) with max(advance, min_advance). This removes a guard that provably\nnever fires, simplifying the seek loop with zero behavioral change, so CER\nstays at the parent's 0.2095 while the pipeline is leaner and clearer.", "fingerprint": ["ablate", "advance-clamp", "dead-code", "seek-floor", "simplify"], "corpus_cer": 0.20948713033476812}
{"iter": 8, "hyp_id": "phase3_007_iter_008", "status": "reject", "capability_investigated": "The previous-text-conditioning path of generate()'s prompt argument — the\ndecode call accepts arbitrary leading token ids, not just the SOT triple, and\nWhisper was trained with a <|startofprev|> prefix carrying prior-window text\nas long-form context. Every prior iter built the prompt purely from\n<|sot|><|ko|><|transcribe|>, so this input was never exercised.", "what_i_learned": "The forced prompt has a second, unused segment: a <|startofprev|>-led prefix\nof raw text token ids placed BEFORE <|startoftranscript|>. The tokenizer\nexposes <|startofprev|> via convert_tokens_to_ids, and the decode call passes\nwhatever id list it is given straight through, so prior-window text can be fed\nback as context. Decoder context is 448 tokens, so the prefix must be capped\n(~half) to avoid starving the current window's emission.", "hypothesis": "Prepend the previous window's text_ids (capped at 200) behind <|startofprev|>\nto each window's prompt. With coverage already at 0.92 the dominant residual\nis 45% substitution; lexical carry-over across the adaptive-seek boundary\nshould stabilize recurring proper nouns and insurance-domain terms and reduce\nboundary substitution, without changing the seek loop or runtime.", "fingerprint": ["startofprev", "prev-text-conditioning", "prompt-prefix", "long-form-carryover", "substitution"], "corpus_cer": 0.285249986929927}
{"iter": 9, "hyp_id": "phase3_007_iter_009", "status": "reject", "capability_investigated": "generate()'s return_scores flag (the per-sequence length-normalized avg\nlog-prob it hands back, discarded every prior iter) and the\nsampling_temperature / sampling_topk sampling path — never exercised since\nthe loop always decoded pure greedy (beam_size=1, temp=0.0, default topk=1).", "what_i_learned": "generate() exposes two unused returns/controls that together implement\nWhisper temperature fallback: result.scores[0] (only populated when\nreturn_scores=True) gives a length-normalized avg log-prob usable as a\nconfidence gate, and sampling_temperature only takes effect when\nsampling_topk != 1 (with the default topk=1 the decoder is argmax and\ntemperature is inert), so the fallback path must set sampling_topk=0 to\nactually sample the full distribution and escape a greedy repetition loop.", "hypothesis": "iter_003 established the residual coverage/deletion loss shares a cause with\ngreedy repetition loops (collapsed run reads as deletion). So decode greedy\nfirst, then re-decode only degenerate windows (zlib compression_ratio > 2.4\nOR avg_logprob < -1.0) at escalating temperature with topk=0 sampling. Clean\nwindows pay nothing; degenerate ones get a non-greedy retry instead of\npoisoning both the text and the timestamp seek target.", "fingerprint": ["temperature-fallback", "return-scores", "compression-ratio", "sampling-topk", "avg-logprob"], "corpus_cer": 0.20910374152623598}
{"iter": 10, "hyp_id": "phase3_007_iter_010", "status": "keep", "capability_investigated": "generate()'s beam-search controls (beam_size, length_penalty) on the\nadaptive-seek parent. length_penalty was discoverable in the frozen\ndocstring but never load-bearing because the loop always decoded greedy\n(beam_size=1), under which CT2's length normalization exponent is inert.", "what_i_learned": "length_penalty is a no-op at beam_size=1 (greedy emits a single hypothesis,\nso there is no length-normalized re-ranking to bias). It only becomes a\ncoverage lever once beam_size>1, at which point the CT2 score is divided by\n((5+len)/6)^length_penalty, so length_penalty>1 favors longer hypotheses —\nthe direct decode-side counter to the dominant deletion axis. Beam search\nalso gives a margin against the greedy repetition loop that collapses the\none repeated_text file into apparent deletion.", "hypothesis": "Switch the per-window decode from beam_size=1 to beam_size=3 with\nlength_penalty=1.1, keeping temperature 0.0. The longer-hypothesis bias\nshould lift length_ratio above the stalled 0.92 and recover deletion, while\nbeam_size=3 (not 5) bounds the ~3x decode cost under the 719.9s budget\n(parent inference was 142.6s).", "fingerprint": ["beam-search", "length-penalty", "coverage", "deletion", "decode-tune"], "corpus_cer": 0.20202847533241552}
{"iter": 11, "hyp_id": "phase3_007_iter_011", "status": "reject", "capability_investigated": "generate()'s return_no_speech_prob flag and the result.no_speech_prob field\nit populates — the decoder's trained probability that a window contains no\nspeech, derived from the <|nospeech|> logit at the first decode step. The\nfrozen docstring advertises this return but every prior iter discarded it\n(the ledger covers mel framing, batched-OOM, beam/repetition kwargs,\ntimestamp tokens, startofprev conditioning, and return_scores/sampling — never\nthe no-speech probability).", "what_i_learned": "Setting return_no_speech_prob=True makes generate() attach a per-window\nno_speech_prob to each result alongside sequences_ids, independent of beam\nsearch. This is the trained gate for the silence-window hallucination the\nper-file diagnosis exposes: files carrying hallucination_hits have very deep\nsilence floors (rms_db_p05 −72 to −82 dB) and long silent runs, so a 30s\nwindow landing in dead air has high no_speech_prob yet still emits invented\ndomain text — that fabrication is what shows up as substitution/insertion and\nalso corrupts the adaptive-seek timestamp. The signal lets that window be\ndropped without touching real-speech windows (which score low).", "hypothesis": "Read result.no_speech_prob each window; when it exceeds 0.8 (set high, not\nWhisper's 0.6, to protect the now-healthy length_ratio 0.93), discard the\nwindow's text and skip a full window past it instead of trusting its\nfabricated last-timestamp. This removes the silent-window hallucination that\nfeeds the dominant substitution axis while leaving genuine coverage intact.", "fingerprint": ["no-speech-prob", "silence-gate", "hallucination", "substitution", "return-flag"], "corpus_cer": 0.20202847533241552}
{"iter": 12, "hyp_id": "phase3_007_iter_012", "status": "reject", "capability_investigated": "generate()'s beam_size width on the family_008 parent, read against the\nparent's measured total_inference_time_s=174.509 — i.e. how much of the\n719.9s runtime budget the current beam=3 decode actually consumes.", "what_i_learned": "The parent's beam=3 adaptive-seek pipeline spends only ~174.5s of the\n719.9s budget (~24%), so the earlier \"beam=3 not 5 keeps decode in budget\"\nconstraint was over-conservative — there is ~4x runtime headroom, enough to\nafford beam_size=5 (~290s est.) without a fallback or stride change. Width\nis a free lever now that coverage (length_ratio 0.93) is healthy and the\ndominant axis is substitution.", "hypothesis": "Raise beam_size 3 -> 5 (length_penalty/temperature unchanged). With the\ndominant axis now substitution rather than deletion, the bottleneck is\nhypothesis re-ranking, not reach: a wider beam keeps more candidate paths\nalive so a correctly-scored Korean insurance/domain term can overtake a\nphonetically-close near-homophone that beam=3 prunes early. The runtime\nheadroom makes the wider search affordable within budget.", "fingerprint": ["beam5", "hypothesis-rerank", "substitution", "runtime-headroom"], "corpus_cer": 0.2129811965216179}
{"iter": 13, "hyp_id": "phase3_007_iter_013", "status": "reject", "capability_investigated": "generate()'s suppress_tokens decode kwarg — a CT2 Whisper.generate parameter\nthe frozen pass-through forwards via **decoding_kwargs but every prior iter\nleft at its default [-1] (the standard non-speech suppression set). Also\nprobed tokenizer.vocab_size and per-id tokenizer.decode to enumerate which\nvocabulary tokens carry out-of-script glyphs.", "what_i_learned": "suppress_tokens accepts an explicit id list and -1 is a sentinel that keeps\nCT2's default non-speech set in force, so [-1, *extra] suppresses the defaults\nPLUS my additions without losing the baseline behavior. Decoding a single\nvocab id gives the glyph(s) that token can emit (partial-byte BPE pieces\ndecode to U+FFFD, which carries no script signal and is correctly skipped),\nso the Han/kana token ids are enumerable once from range(tokenizer.vocab_size)\nand cached. This is a step-level logit constraint, orthogonal to beam/seek.", "hypothesis": "The dominant axis is substitution (46%) with coverage already healthy (0.93),\nand a known large-v3-turbo failure on Korean phone audio is emitting CJK-Han\nideographs / Japanese kana for acoustically ambiguous Korean syllables — each\na guaranteed substitution vs a Hangul reference. Suppressing every out-of-\nscript token forces the decoder onto its best in-script hypothesis, lowering\nsubstitution without touching coverage or runtime.", "fingerprint": ["suppress-tokens", "cjk-script-constraint", "substitution", "logit-mask", "vocab-scan"], "corpus_cer": 0.20297823397173378}
