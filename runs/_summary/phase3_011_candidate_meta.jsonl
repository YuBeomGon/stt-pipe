{"iter": 1, "hyp_id": "phase3_011_iter_001", "status": "reject", "capability_investigated": "The fixed 30s/3000-frame mel produced by WhisperProcessor's feature\nextractor, and generate()'s acceptance of a batched features tensor with a\nper-item prompt list (not just a single window).", "what_i_learned": "The stub's single processor(audio) call truncates every clip to 30s, so all\naudio past 0:30 is silently dropped before decode — the dominant deletion\nsource on long 0715 calls. generate() takes [N,128,3000] batched features\nwith N prompts, so multiple windows decode in one call within budget.", "hypothesis": "Split audio into 30s windows, extract each to its own mel, batch-decode them\nin a single generate() call, and join the per-window transcripts in order to\nrecover the truncated tail and lower deletion-driven CER.", "fingerprint": ["windowing", "30s-chunks", "batched-generate", "long-form"]}
