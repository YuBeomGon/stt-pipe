{"iter": 1, "hyp_id": "phase3_011_iter_001", "status": "reject", "capability_investigated": "The fixed 30s/3000-frame mel produced by WhisperProcessor's feature\nextractor, and generate()'s acceptance of a batched features tensor with a\nper-item prompt list (not just a single window).", "what_i_learned": "The stub's single processor(audio) call truncates every clip to 30s, so all\naudio past 0:30 is silently dropped before decode — the dominant deletion\nsource on long 0715 calls. generate() takes [N,128,3000] batched features\nwith N prompts, so multiple windows decode in one call within budget.", "hypothesis": "Split audio into 30s windows, extract each to its own mel, batch-decode them\nin a single generate() call, and join the per-window transcripts in order to\nrecover the truncated tail and lower deletion-driven CER.", "fingerprint": ["windowing", "30s-chunks", "batched-generate", "long-form"]}
{"iter": 2, "hyp_id": "phase3_011_iter_002", "status": "keep", "capability_investigated": "generate()'s memory behavior when handed a batched [N,128,3000] features\ntensor — specifically how N (window count) scales VRAM, since the parent\npassed every 30s window of a minutes-long call in one call.", "what_i_learned": "generate() accepts arbitrarily many batched windows API-wise, but the\nsingle-call form is not memory-safe: a full minutes-long call produces\nenough 30s windows that one [N,128,3000] decode exhausts CUDA memory\n(RuntimeError: out of memory). Peak VRAM scales with N, so N must be\ncapped — the batched call has a memory ceiling the API does not enforce.", "hypothesis": "Keep 30s windowing (fixes the 0:30 truncation / deletion source) but decode\nin fixed-size sub-batches of 4 windows per generate() call, bounding peak\nmemory independent of clip length. This should run to completion and score\ninstead of OOM-crashing the evaluator.", "fingerprint": ["windowing", "batched-generate", "oom-fix", "chunked-decode"], "corpus_cer": 0.4091891326699545}
{"iter": 3, "hyp_id": "phase3_011_iter_003", "status": "keep", "capability_investigated": "generate()'s **decoding_kwargs beyond beam_size/sampling_temperature — the\nctranslate2 Whisper beam-search controls the parent never set: patience,\nlength_penalty (length normalization exponent), and no_repeat_ngram_size.", "what_i_learned": "The parent decodes every 30s window with beam_size=1 (pure greedy) and only\nsampling_temperature=0.0, leaving the whole beam-search surface unused. ct2's\ngenerate accepts patience, length_penalty and no_repeat_ngram_size on the same\ncall; length_penalty normalizes the beam score by pow(len, lp), so lp>1 rewards\nlonger sequences — a knob aimed straight at the deletion/coverage axis that\ngreedy decode cannot reach. Deletion (80%) and length_ratio ~0.65 are uniform\nacross files, not just repeated_text ones, so undergeneration is systematic.", "hypothesis": "Replace greedy per-window decode with tuned beam search (beam_size=5,\npatience=2.0, length_penalty=1.1, no_repeat_ngram_size=3). Wider beam + patience\nrecover content that greedy drops via early EOS on quiet/noisy windows,\nlength_penalty>1 favors longer hypotheses to raise length_ratio toward 1.0, and\nno_repeat_ngram_size=3 suppresses the repetition collapse flagged as\nrepeated_text — all targeting the dominant coverage/deletion axis.", "fingerprint": ["beam-search", "patience", "length-penalty", "no-repeat-ngram"], "corpus_cer": 0.3552184444870432}
{"iter": 4, "hyp_id": "phase3_011_iter_004", "status": "reject", "capability_investigated": "generate()'s decode-prompt control over timestamp emission, and the timestamp\ntokens that appear inside results[0].sequences_ids when <|notimestamps|> is\nabsent — information the pipeline currently discards via skip_special_tokens.", "what_i_learned": "The prompt list fully controls timestamp mode: omitting <|notimestamps|> makes\nthe model emit <|t.tt|> segment-timestamp tokens (ids >= the <|0.00|> token id)\ninterleaved in sequences_ids, each encoding time = (id - ts_begin) * 0.02 s\nrelative to the window start. This exposes WHERE each decoded segment ends —\nenabling reference-style seeking instead of fixed 30s hops. It is reachable\nthrough the frozen surface with no new import.", "hypothesis": "Deletion is concentrated at the hard 30s cuts that slice utterances the model\nthen can't reconstruct. Decode each window with timestamps on, keep text only\nup to the last segment-end timestamp, and advance the next window's start to\nthat time — windows snap to segment boundaries so no utterance is sliced and\nno overlap is duplicated, lifting length_ratio against the coverage axis.", "fingerprint": ["timestamps", "segment-seek", "sequential-windowing", "coverage"]}
{"iter": 5, "hyp_id": "phase3_011_iter_005", "status": "reject", "capability_investigated": "The `prompts` argument to generate() — not its decode kwargs but the token\nprefix itself. Whisper's prompt accepts a previous-text conditioning block\n(<|startofprev|> + prior text token ids) placed BEFORE the\n<|startoftranscript|> SOT sequence; the pipeline has only ever passed the\nbare 4-token SOT sequence.", "what_i_learned": "generate()'s per-item prompt can encode condition_on_previous_text: prepending\n<|startofprev|> + recent text token ids (those with id < the <|endoftext|> id,\ni.e. non-special text tokens) to the SOT suffix is accepted by the same\nbatched call with no new import and no extra VRAM (encoder features unchanged,\nonly the prompt grows). This is an input axis distinct from the timestamp\nOUTPUT probe in iter_004 — prompt-prefix conditioning, never used before.", "hypothesis": "Fixed 30s windows decode each batch as a cold start from silence, dropping the\nleading partial utterance at every boundary — a uniform deletion source\nmatching del 77% / len 0.71. Carrying the previous batch's decoded text as a\n<|startofprev|> prompt prefix (capped at 200 text tokens) primes the model\nacross boundaries, recovering boundary-cut content and raising length_ratio\nwithout changing the batched-decode memory ceiling.", "fingerprint": ["startofprev", "prev-text-conditioning", "prompt-prefix", "boundary-coverage"]}
{"iter": 6, "hyp_id": "phase3_011_iter_006", "status": "reject", "capability_investigated": "generate()'s decoder KV-cache memory scaling under prev-text prompt\nconditioning: how peak VRAM depends on (window-batch x beam x prompt+gen\nseq_len) once <|startofprev|>+prev_tokens is prepended to the SOT suffix.", "what_i_learned": "iter_005's OOM was not from the encoder (features stay [N,128,3000]) but from\nthe decoder: prepending ~200 prev-text tokens lengthens every beam sequence,\nand at batch=4 x beam=5 = 20 concurrent sequences that pushed the KV-cache\npast VRAM. Peak decoder memory ~ batch x beam x seq_len, so halving the window\nbatch (10 sequences) more than offsets the longer prompt — below iter_003's\nworking 20-sequence footprint. Prompt growth is a memory axis distinct from\nwindow count (iter_002's finding).", "hypothesis": "Keep iter_005's previous-text conditioning (cross-window context to recover\nboundary deletions) but set _BATCH_WINDOWS=2 and _PREV_CONTEXT_TOKENS=100 so\nthe longer prompts stay within the VRAM ceiling, letting the conditioning\nactually be evaluated against the coverage axis.", "fingerprint": ["prev-text-conditioning", "oom-repair", "batch-windows-2", "kv-cache-bound"]}
